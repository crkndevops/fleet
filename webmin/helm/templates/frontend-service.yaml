apiVersion: v1
kind: Service
metadata:
  name: webmin
  labels:
    app: webmin
    tier: allcloud
spec:
  type: "{{ .Values.serviceType }}"
  ports:
  - port: 10000
  selector:
    app: webmin
    tier: allcloud
